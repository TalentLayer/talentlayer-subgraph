{
  "name": "talent-layer",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ talentlayerid/talent-layer",
    "create-local": "graph create --node http://localhost:8020/ talentlayerid/talent-layer",
    "remove-local": "graph remove --node http://localhost:8020/ talentlayerid/talent-layer",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 talentlayerid/talent-layer"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.22.4",
    "@graphprotocol/graph-ts": "0.22.1"
  }
}
